[theme]
# Cor azul corporativa (substitui o botão vermelho)
primaryColor = "#1E40AF"

# Fundo geral da tela (Preto profundo que queríamos)
backgroundColor = "#050810"

# Fundo da barra lateral e dos Cards (Azul acinzentado, criando o contraste)
secondaryBackgroundColor = "#1E293B"

# Cor dos textos globais
textColor = "#F8FAFC"

font = "sans serif"
